[project]
name = "bee-hive-core"
version = "0.1.0"
description = "Bee-Hive core types and configuration"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/bee_hive_core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
